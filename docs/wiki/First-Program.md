# First Program

> Mirrored from <https://thefarmerwasreplaced.wiki.gg/wiki/First_Program>
> — CC BY-SA. Regenerate with `python3 tools/fetch_wiki.py`.

## Text editor

All programming is done in code windows. Each code window corresponds to a text file containing code. You can rename the file by clicking on its name at the top of the window.

The code can be edited like in any text editor as long as it's not running. You can execute the program directly by pressing the green play button in the code window.

<figure>
<img src="PlayButton.png" title="PlayButton.png" />
<figcaption>PlayButton.png</figcaption>
</figure>

When you execute a program the code blinks so you can see the execution flow.

You can create more code files using the "+" button in the top right corner of the screen. You can dock a window onto another window by dragging it onto it.

You will notice that once you start typing, a simple code completion window will pop up. Press tab to insert the code completion. Use the arrow keys to navigate the completion options.

Don't worry if this is your first time programming. The language is unlocked step by step, so you don't get overwhelmed by all the things you can do. The syntax is also similar to that of Python, which is one of the most widely used programming languages in the world, so learning it is not completely wasted.

If you already know Python, it's not a problem either, you'll just be able to skip the early game quickly to get to the more interesting stuff.

Currently, there are two drone commands available.

`harvest()`

and

`do_a_flip()`

These are function calls. You can think of a function as a command that can be executed. You execute it using the `()` parentheses.

Try typing these statements in the code window and pressing the execute button.

You can think of your code as a sequence of statements. You can run multiple statements in a row like this:

<div id="cb1" class="sourceCode">

``` sourceCode
harvest()
do_a_flip()
harvest()
```

</div>

## Unlocks

Collecting grass will give you hay. Hay can be used to unlock loops in the unlock menu. Open the unlock menu with the button in the top right corner.
