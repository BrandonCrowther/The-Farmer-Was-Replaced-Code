# Maze_Single — experiment queue

Target: **616_448 gold** on an 8x8 farm with a single drone
Entry point: `main` · Runner: `leaderboard_run(Leaderboards.Maze_Single, "main", 5000)`

Branches: `auto_experiment/maze_single/NNN` · Results: `experiments/maze_single/NNN/result.md`

## Queued

- [ ] 001 write-driver — no single-drone/8x8 code exists for this category.
      Write a driver that hits the target on an 8x8 farm with one drone and
      terminates. Metric: the run completes and reports a time.

## Done

_(nothing yet)_
