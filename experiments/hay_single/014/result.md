# exp-014 — adjacent-tiles-reroll-guard — result

**Outcome.** superseded, not run. While preparing this (2-tile adjacent,
same-tile guard), the user's own proposal (4 clustered tiles, full-board
Bush blanket, reroll-until-Bush) arrived as a more literal, more
generous test of the same underlying question — run instead as 015, which
gives 014's hypothesis a stronger and more direct answer than running this
narrower version would have. See `experiments/hay_single/015/result.md`.
