# exp-011 — no-polyculture

**Hypothesis.** The companion preference rerolls every pass (007), so the
companion walk costs ~800 ticks *every* pass and cannot be amortised. A pass
without it is ~200 ticks. Polyculture therefore has to be worth about 5x to break
even. This run prices it.

**Variable.** `Common.polyculture()` not called. Nothing else — 008 removed
polyculture *and* added a 25-tile circuit, so its result attributes to neither.

**Metric.** Time vs 03:24.552, or the hay rate if the run is too slow to finish.
